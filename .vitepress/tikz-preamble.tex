% Shared preamble for all ```tikz fences.
% Changing this file invalidates the diagram cache (restart `npm run dev`
% or touch the page to see the effect).
\usetikzlibrary{calc, arrows.meta, intersections, decorations.pathreplacing}
